{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "vite build && esbuild serverless/trpc.ts --bundle --platform=node --format=esm --packages=external --outfile=\"api/trpc/[[...path]].js\" && esbuild serverless/cron-rank-check.ts --bundle --platform=node --format=esm --packages=external --outfile=api/cron/rank-check.js",
  "outputDirectory": "dist/public",
  "framework": null,
  "functions": {
    "api/**/*.js": { "maxDuration": 60 }
  },
  "crons": [
    { "path": "/api/cron/rank-check", "schedule": "0 6 * * 1" }
  ],
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
